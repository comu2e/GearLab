version: '3'

services:
  php:
    container_name: php
    build: .
    volumes:
      - .:/var/www

  nginx:
    image: nginx
    build: ./docker/nginx
    container_name: nginx
    ports:
      - 80:80
    volumes:
      - .:/var/www
    depends_on:
      - php
